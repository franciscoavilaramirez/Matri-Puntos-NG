import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matripuntos.app',
  appName: 'MatriPuntos',
  webDir: 'dist/matripuntos-ng/browser'
};

export default config;
